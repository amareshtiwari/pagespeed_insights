# Secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key

# Dependencies
node_modules/

# Runtime / local data overrides (keep sample files in repo)
data/cron.log
data/*.local.json

# OS & editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.swp

# Cursor / local tooling
.cursor/
agent-transcripts/
